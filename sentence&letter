public static void main(String[] args) {
		/*
         * Take a sentence and letter from user and find the letter how many times used in the sentence
         *
         * example:
         *
         * input : Football is an overrated sport.
         *
         * output : your sentence contains your letter 3 times
         */

		Scanner scan = new Scanner(System.in);
		System.out.println("enter a full sentence");
		String sentence = scan.nextLine();
		
		System.out.println("enter a letter");
		char letter = scan.next().toLowerCase().charAt(0);

		int count = 0;
		
		for (int i = 0; i < sentence.length(); i++) {
			
			if (sentence.charAt(i) == letter) {
				count++;
			}
			
		}
	System.out.println("your sentence contains your letter " + count + " times");
		
		
		
	}

}
